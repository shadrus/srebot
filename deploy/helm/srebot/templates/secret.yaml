{{- if empty .Values.secrets.existingSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ai-observability-bot.fullname" . }}-secrets
  labels:
    {{- include "ai-observability-bot.labels" . | nindent 4 }}
type: Opaque
data:
  # Base64 encoded values
  TELEGRAM_BOT_TOKEN: {{ .Values.secrets.telegram_bot_token | b64enc | quote }}
  TELEGRAM_CHANNEL_ID: {{ .Values.secrets.telegram_channel_id | quote | b64enc | quote }}
  SAAS_AGENT_TOKEN: {{ .Values.secrets.saas_agent_token | b64enc | quote }}
{{- end -}}
