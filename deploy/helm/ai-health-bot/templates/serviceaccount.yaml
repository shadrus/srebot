apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ai-health-bot.fullname" . }}
  labels:
    {{- include "ai-health-bot.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.serviceAccount.create }}
